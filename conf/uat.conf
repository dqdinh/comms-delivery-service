include "common.conf"

mailgun {
  domain = "@@{uat.mailgun.domain}"
  apiKey = "@@{uat.mailgun.api_key}"
}

kafka {
  hosts = "@@{uat.kafka_hosts}" # comma-separated, e.g. "host1:9092,host2:9092"
}

email {
  blacklist = [
    "some@email.com"
  ]
}