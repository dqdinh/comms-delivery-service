twilio.retry {
  attempts = 5
  interval = 1 second
}

dynamo{
  aws-region = "eu-west-1"
  retry-config{
    interval = 1 second
    attempts = 5
  }
}

kafka.retry {
  attempts = 6
  initial-interval = 2 second
  exponent = 2
}

mailgun.retry {
  attempts = 7
  interval = 3 second
}